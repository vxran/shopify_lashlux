{% assign currencySize = 4 %}
{% assign currencyCodes = "USD EUR GBP CAD AUD " | rstrip | split: ' ' %}
{% assign currencySymbols = "$ € £ $ $ " | rstrip | split: ' ' %}
{% assign defaultCurrencyCode = "USD" %}
{% assign defaultCurrencySymbol = "$" %}

    {% if shop.enabled_currencies.size > 1 %}
        {% render 'scm-currency-switcher-08' %}
        <style>
            .sca-currency-style,.select-currency{
                display: none;
            }
            .sca-body-currency form{
                margin: 0 !important;
            }
                .cs-select .cs-options {
            width: 100%;
        }
        {% if shop.enabled_currencies.size > 7 %}
            .cs-skin-elastic .cs-options > ul > li {
                position:  absolute;
            }
            .cs-skin-elastic.cs-active .cs-options > ul > li {
                position: relative;
            }
            .cs-skin-elastic.cs-active .cs-options {
                width: 100%;
                max-height: 290px;
                overflow: auto;
            }
        {% endif %}
                </style>

    {% endif %}
